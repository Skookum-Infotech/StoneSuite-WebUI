// The surface every Support panel sits on — the ticket inbox, the new-ticket
// form and its guidance cards: a flat white card on the page's warm canvas,
// separated by a hairline border rather than a shadow (tonal layering, per the
// design system).
export const FEEDBACK_CARD_CLS =
  'rounded-xl border border-stone-200 bg-white dark:border-white/10 dark:bg-white/[0.03]';
