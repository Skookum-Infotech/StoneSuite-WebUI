name = "stonesuite"
type = "javascript"
account_id = ""
workers_dev = true

# Build configuration
build = { command = "npm run build" }

# Functions (Cloudflare Pages Functions)
# Wrangler will look for Worker functions in this directory
# and deploy them alongside the static site
[functions]
directory = "functions"

# Environment-specific configuration can go here
# [env.dev]
# [env.uat]
# [env.production]
